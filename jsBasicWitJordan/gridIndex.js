function gridIndex(grid, indices) {
  let strs = " "
  for (let i = 0; i < grid.length; i++) {
    for (let j = 0; j < grid[i].length; j++) {}
  }
  // var sum = Math.pow((2*(grid.length-1)+1), 2
  // return (indices[0] + 1) * ((2*grid.length+3))
}

// function to get the index of a cell in an n x m matrix given its row
// and column coordinates
